# M8.11 method note: local branch germs at the level-6 critical rays, and the order-a² tilt

> Task record: [`../tasks/m8_11_task_details.md`](../tasks/m8_11_task_details.md) (frozen claims, go-time pre-registration, deviations log). Roadmap: [`../m8_roadmap.md`](../m8_roadmap.md) M8.11. Run 2026-09-18, go 11:50 EDT, maintainer-run, both rooms headless. Labels earned: **blind** for every value in the roadmap's sense ([§ CONVENTIONS](../m8_roadmap.md#conventions)), since no agent saw a claimed value; **audited argument** for T1, S1 and C2's lemma, per the go-time grading rule.

## 1. Equations first

### 1.1 The problem

On sections of the flat unitary bundle `E_σ` over `X = S³/Γ`, `σ ∈ {3′, 4}`, with `Γ` generated by the two unit quaternions of the handout ([`../m8_11/worklist.md`](../m8_11/worklist.md) § 1.2):

```text
F(ψ, λ) = (−Δ − λ) ψ + g N(ψ) = 0,        N(ψ) = |ψ|² ψ   (pointwise),   g real, g ≠ 0
```

Level `n` carries `−Δ = n(n+2)`, the Haar measure has `∫_X 1 = 1`, and the block `K` is the seven-dimensional level-6 space. A fibre vector `u ∈ V_3` lifts to the block section `Φ = Φ_{σ,u}`, normalized so that `∫_X |Φ|² = 1`. `F` is the `⟨·,·⟩_ℝ`-gradient of `E_λ(ψ) = ½⟨ψ, (−Δ − λ)ψ⟩ + (g/4)∫|ψ|⁴`, which is invariant under `G = SU(2)_left × U(1)`.

### 1.2 The expansion with a tilt

```text
ψ = aΦ + a³(κ + ξ) + a⁵ζ + …,      λ = 48 + λ₂ a² + λ₄ a⁴ + …,      κ ∈ K, ⟨Φ, κ⟩ = 0,  ξ, ζ ⊥ K

order a³, block:   Π_K N(Φ) = Q Φ,  λ₂ = g Q,  Q = ∫|Φ|⁴           (Φ critical for Q on ℙ(K))
order a³, range:   A ξ = −g Π_⊥ N(Φ),   A = −Δ − 48
order a⁵, block:   g Π_K DN_Φ[κ + ξ] − λ₂ κ − λ₄ Φ = 0,   DN_Φ[h] = 2 Re(Φ†h) Φ + |Φ|² h
   tangential:     L_T κ = −Π_T DN_Φ[ξ],   L_T = Π_T DN_Φ − Q  on the slice tangent T
   along Φ:        λ₄ = g ⟨Φ, DN_Φ[ξ]⟩ = −3 g² Σ_n ‖Π_n N(Φ)‖² / (n(n+2) − 48)
```

**The bridge.** For a unit tangent `E ⊥ Φ` in the block, `d²Q(cos s·Φ + sin s·E)/ds² = 4(⟨E, DN_Φ E⟩_ℝ − Q)` at `s = 0`, so `L_T = ¼ Hess_T Q`, and `Q = 1 + w₆(σ) r̂₆` gives `L_T = (w₆/4) Hess_T r̂₆`, with `w₆(3′) = 28/39` and `w₆(4) = 21/52`. One set of numbers decides both the nondegeneracy of the reduced problem and the tilt.

### 1.3 The six rays and the two new tangents

`U1 = v_3`, `U2 = v_0`, `U3 = v_2 + v_{−2}`, `U4 = v_3 + v_{−3}` (the coherent, zonal, octahedron and hexagon rays), `U5 = cos t·v_2 + sin t·v_{−3}` with `sin²t = 12/25` (the pentagonal pyramid) and `U6 = v_3 + z₀ v_0 + v_{−3}` with `z₀ = √(23/10)` (the trigonal prism). At U5 the tangent `e_t` is the unit section of `−sin t·v_2 + cos t·v_{−3}`, and `i·e_t` is a rotation orbit. At U6, `τ_x` is the unit section of the part of `v_0` orthogonal to U6, and `τ_y = i·τ_x`.

### 1.4 The existence theorem, in one line

Blow up (`ψ = a(Φ + k) + a³w`, `λ = 48 + a²μ`), divide by `a³` to remove the trivial branch `ψ = 0`, solve the range equation by the implicit function theorem on `K^⊥`, and solve the reduced block equation on a slice transverse to the `G`-orbit, where `L_T` is invertible. The theorem as the author stated it (T.a to T.e) is transcribed in the stage-2 packet [`../m8_11/theorem_stage2.md`](../m8_11/theorem_stage2.md) and graded in § 5.

## 2. What the agents derived, none of it supplied

| Result | Solver | Auditor |
| --- | --- | --- |
| Sector levels (item 1) | DERIVED from exact characters in `Q(√5)` | DERIVED from class-sum projectors and exact characters |
| Values | exact SymPy for every quantity built from rational `Γ`-constants (`r̂₆`, `Q`, level norms, `λ₄`, item 6, item 11); item 5 and 7 components identified at 80 and 120 digits (denominators up to `10³⁰`, fields `ℚ`, `√39·ℚ`, `√115·ℚ`, `√3·ℚ`) | exact throughout, in multi-quadratic arithmetic with its own Clebsch-Gordan coefficients (two constructions, 3,541 coefficients, no mismatch) |
| The antiunitary symmetry (item 9) | `A′ = −L_{R_y(π)}∘C`, complex conjugation with a real `σ` and `Θ`-real intertwiners, acting on block fibres as `u ↦ ū` | the same map, `S′ = −r_y(π)∘T`, with `W ∈ Hom_Γ(σ, σ̄)` derived and checked exactly |
| The sign of `λ₄` (item 8) | Cauchy-Schwarz: `Q² ≤ ∫\|Φ\|⁶`, strict because `\|Φ\|²` has a nonzero level-12 part | every `μ_n > 0` (no level below 6), and exact nonzero terms at levels 14 and 18 |
| The level-16 zero at U4 | `Sym³(V₃)` has no spin 8 | the same, stated as an identity of the cubic map |
| Existence (item 10) | Lyapunov-Schmidt, blow-up and division by `a³`, a slice with Noether identities, `ker L` = the orbit tangent by exact Hessian rank | Lyapunov-Schmidt with blow-up, symmetric criticality on a slice, and a `Fix(Ĥ)` route for U1 to U4 |

Both agents found the tilt, the order-`a⁵` breakdown without it and the null orbit direction at U5 with the handout asking the same questions at all six rays.

## 3. Equation-to-code map

Reproduction: each folder's `run_all.py`, run as `./py run_all.py` from inside the folder, with a `py` beside it that starts Python 3.12 without site processing, numpy, scipy, sympy and mpmath on the path and the math libraries pinned to one thread.

### 3.1 The solver, [`../scripts/m8_11_solver/`](../scripts/m8_11_solver/)

| Equation | Code |
| --- | --- |
| `Γ` from the generators, perfectness, `⟨3 3; 3 −3 \| 6 0⟩` (X0) | [`s00_group.py`](../scripts/m8_11_solver/s00_group.py), on the exact `Q(√5)` quaternions of [`common.py`](../scripts/m8_11_solver/common.py) (`qmul`, `closure`, `group`) |
| Levels, intertwiners, residuals (item 1) | [`s01_levels.py`](../scripts/m8_11_solver/s01_levels.py) |
| `r̂₆`, its Hessian, the level norms and `λ₄` exactly (1.2, the bridge) | [`s02_su2_exact.py`](../scripts/m8_11_solver/s02_su2_exact.py) (`rho`, `h_along`, `d_along`) |
| Stabilizers and characters (T2) | [`s03_stabilizers.py`](../scripts/m8_11_solver/s03_stabilizers.py) (Majorana roots, `lie_stabilizer_dim`, `isotypic_dim`) |
| `ξ`, the order-`a⁵` block projection, `L_T`, `κ`, `λ₄` with and without `κ` | [`s04_pipeline.py`](../scripts/m8_11_solver/s04_pipeline.py) (`analyze`, `W_basis`, `antiunitary`) |
| Reasons for every zero | [`s09_zeros.py`](../scripts/m8_11_solver/s09_zeros.py) |
| Hypotheses of the existence argument, where verified | [`s10_existence.py`](../scripts/m8_11_solver/s10_existence.py) |
| Order-`a³` residual with an explicit Casimir, `N(Φ)` pointwise (X4) | [`s12_quadrature.py`](../scripts/m8_11_solver/s12_quadrature.py) (`minus_laplacian`, `project_quadrature`) |
| Independence of the quaternion embedding | [`s13_identification_B.py`](../scripts/m8_11_solver/s13_identification_B.py) |
| Negative controls for every kind of PASS line | [`s14_negative_controls.py`](../scripts/m8_11_solver/s14_negative_controls.py) |

### 3.2 The auditor, [`../scripts/m8_11_audit/`](../scripts/m8_11_audit/)

| Equation | Code |
| --- | --- |
| Its own coupling coefficients and `D^j`, two constructions | [`su2.py`](../scripts/m8_11_audit/su2.py) (`cg`, `cg_table_hw`, `D_exact`) |
| Exact multi-quadratic field | [`mq.py`](../scripts/m8_11_audit/mq.py) |
| Sections as sums of `xᵀ D^J(g) Y`, products, `N`, `DN` | [`engine.py`](../scripts/m8_11_audit/engine.py) (`trilinear`, `N`, `DN`) |
| `Γ`, derived subgroup, isotypic projectors, levels (X0, item 1) | [`s01_group.py`](../scripts/m8_11_audit/s01_group.py), [`s02_intertwiners.py`](../scripts/m8_11_audit/s02_intertwiners.py) |
| Stabilizers and characters (T2) | [`s03_stabilizers.py`](../scripts/m8_11_audit/s03_stabilizers.py) |
| Items 2 and 4 to 8, exact, at the six rays and N1's point | [`s_main.py`](../scripts/m8_11_audit/s_main.py) (`analyze`) |
| Structural checks behind the arguments, each with a mutation | [`s_verify.py`](../scripts/m8_11_audit/s_verify.py), [`checks.py`](../scripts/m8_11_audit/checks.py) |
| Pointwise `N(Φ)` by Haar quadrature, the explicit Casimir (X4) | [`s12_quadrature.py`](../scripts/m8_11_audit/s12_quadrature.py) |

## 4. Results against the frozen claims

Designer comparison, scripted against a transcription of the frozen claims that was checked before launch ([§ Go-time pre-registration](../tasks/m8_11_task_details.md#go-time-pre-registration-2026-09-18-go-1150-edt)): 104 of 104 mechanical checks pass for each agent. The comparison decides equality symbolically and was mutation-tested before use: a denominator off by one, the `√230` field of X1, a sign flip of X5, the coordinate-tangent metric of X1 and a wrong dimension each fail it.

| Claim | Solver | Auditor | Verdict |
| --- | --- | --- | --- |
| X0 packet check | order 120, perfect | order 120, perfect | ✅ precondition met |
| A1 critical data on both loci | `9/35` at `s = 12/25`, `200/903` at `z₀`, stationary in the whole block | same | ✅ |
| A2 `λ₂/g` at the two new rays | all four | all four | ✅ |
| A3 `F = −√(d/7)·R` against M8.10's recorded `R` | both sectors | both sectors | ✅ |
| T2 `dim K_H` = 1, 1, 1, 1, 2, 2 | all six | all six | ✅ |
| T3 `ξ ∈ X_H` | all four new expansions | all four | ✅ |
| T4 `Hess_T r̂₆`, `L_T`, null orbit direction, no cross term | all 20 entries | all 20 entries | ✅ |
| B1 the 18 level norms | all exact, none zero | all exact, none zero | ✅ |
| C1 `λ₄/g²` | all four | all four | ✅ |
| C2 `λ₄` unchanged by the tilt, `λ₄ < 0` argued | identical with and without `κ`, Cauchy-Schwarz | identical, positive denominators | ✅ audited argument (§ 5.3; the auditor's DEFECT on the author's text overruled, reason stated) |
| D1 the forcing, zero along `i·e_t` and `τ_y` | all eight | all eight | ✅ |
| D2 the tilt, with signs | all eight | all eight | ✅ |
| D3 `‖v‖²/g²` | all four | all four | ✅ |
| D4 the tangential residual, zero with the tilt, `\|F\|` without | exact zero (residual `1e-123` at 120 digits) | exact zero | ✅ |
| N1 non-critical point, pairing nonzero and on its recorded value | both sectors | both sectors | ✅ |
| X4 order-`a³` residual, `N(Φ)` pointwise | `1.5e-29` at 30 digits | `8.5e-15` in float64 | ✅ |
| T1 existence, the author's proof | diagnostic: item 10 complete (6 of 7 elements) | stage-1 argument establishes the result; T.a to T.e all ESTABLISHED | ✅ audited argument |
| S1 the prism's S lemma | found a different antiunitary symmetry (§ 5.4) | lift derived, every consequence checked exactly | ✅ audited argument |

The values at U1 to U4, which M8.10 already graded, agree between the two agents exactly and are not re-graded.

## 5. Adversarial audit record

The auditor committed its method ([`METHOD.md`](../scripts/m8_11_audit/METHOD.md)), its own existence argument and its own results ([`audit_stage1_return.md`](../scripts/m8_11_audit/audit_stage1_return.md)) before it saw anything else. A second headless session in the same room then received the solver's work and the author's theorem text ([`../m8_11/theorem_stage2.md`](../m8_11/theorem_stage2.md)), and returned [`audit_stage2_return.md`](../scripts/m8_11_audit/audit_stage2_return.md). Its 72 stage-1 files were byte-identical after stage 2.

### 5.1 Reproduction and verdicts

The solver's `run_all.py`, rerun from a copy, printed 503 PASS and 0 FAIL and regenerated `results.json` and all eleven per-script outputs byte-identical. Of 490 paired values, all 490 agree exactly. Items 0 to 8 and 10 to 12 are CONFIRMED; item 9 is PARTIAL, for the reasons in 5.2.

### 5.2 Checks that cannot fail

Each mutation ran in a fresh copy of the solver's code.

| Mutation | Result |
| --- | --- |
| m1, m2, m5: break the antiunitary map's verification, or plant zeros at points with no such symmetry | the item 9 reason lines still PASS: the reason script attaches labels without reading any verification. The upstream antiunitary check does fail (m5), and the prose proofs carry the weight |
| m3: insert levels 2 and 4 into the 4-dimensional sector | the spectral-gap line still PASSes, since `n(n+2) = 48` only at `n = 6`. No line checks "no level below 6", which the sign of `λ₄` needs |
| m4, m9: a wrong character; wrong even-level dimensions | lines implied by neighbouring checks, which do fail |
| m10, m11: a wrong character; every `λ₄` made positive | two `results.json` fields (`xi_transforms_by_same_character`, `sign_of_lambda4`) are literals in the assembly script and do not move |
| m6, m7: a wrong pseudo-inverse; a wrong Casimir factor | real checks: both fail |

No number is affected: the auditor reproduced every value by its own exact route. The solver's identified values (the item 5 and 7 components and `‖κ‖²`) and its exact SymPy values share one section algebra, so its internal agreement lines cannot see an error in that algebra; the auditor's independent exact values close that gap.

### 5.3 The arguments, graded

Per step, under the go-time grading rule. The auditor's full reasoning is in its stage-2 return, § Task 5.

| Statement | Auditor's grade | Designer's reading |
| --- | --- | --- |
| T.a function spaces | ESTABLISHED | agreed. "Nearest other level" does not by itself exclude levels below 6; item 1 does, and T does not need it |
| T.b the fixed space | ESTABLISHED; `ξ ∈ X_H` checked exactly | agreed |
| T.c the reduction | ESTABLISHED, with one reading made explicit: "tangential part" is the component in the moving tangent space, the `θ`-gradient of the reduced functional; with the fixed projection, `λ` is substituted from the radial equation first, and the Jacobian is the same | agreed: T.c itself says the reduced equations are the gradient of the reduced functional, which is the moving-frame reading |
| T.d the statement | ESTABLISHED; uniqueness within `X_H`, as stated | agreed |
| T.e identification | ESTABLISHED; needs the analytic reparametrization from `‖k‖` to `⟨Φ, ψ⟩` | agreed; a supplied step, not a gap |
| E1, E2 | ESTABLISHED; `m = 1` at U1 to U4 and `m = 2` at U6 certified exactly, the U5 weights exact, no continuous symmetry at U6 | the auditor noted E2 does not state that `Hess_T Q` is invertible. The frozen task doc does (T4's table); the stage-2 transcription omitted those value paragraphs, so this is a packet artifact |
| The bridge | ESTABLISHED; `Q″` from `N` alone equals 4 × the item 6 form in all 12 cases | agreed |
| The `λ₄` lemma | **DEFECT**: "`λ₄ < 0` since `Q ≠ 1`" leaves unstated that every non-block level lies above 6 | **overruled, reason stated**: the frozen text reads "By M8.10's argument", and that argument rests on M8.10's blind-graded level sets (all levels other than 6 lie above it). The stage-2 transcription replaced the citation with "an earlier argument, not supplied here", which removed the hypothesis's source (deviations log). The auditor's rescue is exactly that hypothesis plus strict Cauchy-Schwarz. ⚠️ Standing alone, the sentence is incomplete, and the sum as written includes `n = 6`, where `μ₆ = 0` |
| The S lemma | ESTABLISHED; the section-level lift derived (`conj(P) = RPR`, exact), `S: u(z) ↦ u(z̄)` with no phase, and every consequence checked exactly | agreed. Two remarks: the phase freedom is taken in the lift or in `η`, not in `u` alone, and `v_y = 0` also uses `L_T(τ_y, τ_y) ≠ 0` |

**Verdicts.** T1 PASS: the auditor's stage-1 argument establishes the result, divides out the trivial branch by the blow-up before the implicit function theorem and says what fails without it, and every step of T.a to T.e is ESTABLISHED. S1 PASS. C2 PASS: both agents derived the lemma from criticality, the two `λ₄` agree exactly with and without the tilt, and both argued the sign, the solver for every critical `Φ` in either sector. All three are **audited arguments**. The solvers' item-10 diagnostic scores 6 of 7 elements (no twisted fixed space: the solver works on a slice modulo `G`), complete; the auditor's 7 of 7.

### 5.4 The arguments compared

| Point | Finding |
| --- | --- |
| The antiunitary symmetry | both agents found `−R_y(π)` composed with fibre conjugation, acting as `u ↦ ū` and fixing all six real fibre vectors. The author's `S = R_z(π/3)∘Θ` acts as `z ↦ z̄` on the prism chart: a different map, graded separately and ESTABLISHED |
| The sign of `λ₄` | the solver's Cauchy-Schwarz argument holds at every critical `Φ`; the auditor's used the computed level norms and holds at the six points |
| The level-16 zero at U4 | not forced by the stabilizer (its character subspace in `V₈` is one-dimensional); forced because `Sym³(V₃)` has no spin 8 |
| Existence | the same architecture in both agents. The auditor proves uniqueness modulo `G` in a cone around the orbit, stronger than T's uniqueness within `X_H` |
| One audit incident | the auditor re-imported one stage-1 script at stage 2, which rewrote three output files byte-identical to its stage-1 clean run; it disclosed this, and the stage-1 hashes confirm it |

## 6. Containment record

| Check | Result |
| --- | --- |
| Launch | both rooms headless under [`clean_room_launch.sh`](../../../../../dev_docs/utils/clean_room_launch.sh), in neutral folders under `/tmp`; every launch check green. Init records: `claude-opus-5`, four tools, no MCP server, no skill, no memory path |
| Solver transcript | 85 tool calls, 51 minutes. Outside the room: three reads of the output files of its own background commands, which the harness writes to a per-session folder holding only those files |
| Auditor stage 1 | 53 tool calls, 32 minutes, nothing outside the room |
| Auditor stage 2 | 71 tool calls, 28 minutes. Outside the room: one read of its own oversized tool output, which the harness saved to a per-session folder and pointed the session at; the folder holds only that file. Its manifest says "nothing outside the room", so it did not register the read as leaving |
| Shell | the shell also ran in-room writes by redirection and a `time` prefix, outside the interpreter sandbox; every path stayed in the room, and four commands were refused automatically |
| Audit pattern | mutation-tested before use on eight synthetic out-of-room calls, all flagged, and five in-room calls, none flagged |

⚠️ Both outside reads are harness pointers: a background-output file and a saved oversized result. `--restricted` allows them. Neither carried anything but the session's own output, but the route is open in principle, and [§ 3.4](../../../../../dev_docs/CLEAN_ROOM_STANDARDS.md) should name it.

## 7. Provenance

The author's package landed after this verdict, at [#566](https://github.com/openwave-labs/openwave/pull/566), byte-identical to the fifteen hashes in the task doc's firewall section, and the comparison against it is recorded in the task doc's [provenance comparison](../tasks/m8_11_task_details.md#provenance-comparison-2026-09-19). Agreement with it is weak evidence, since a shared convention error survives it, and disagreement is strong evidence.

## 8. What this run does NOT verify

| Not verified | Why |
| --- | --- |
| Any radius: how small `a` must be | T gives `ε > 0` with no lower bound, and none was sought |
| Stability of any kind | deferred to its own decision by the task |
| Branches at finite amplitude | the result is local germs, for sufficiently small amplitude |
| The weight states `v_1`, `v_2` and the rest of the critical set | out of scope by the task's own statement |
| T1 as a theorem | an audited argument: checked step by step by one AI auditor and read by the designer, with AI agents on both the drafting and the checking side |
| Values under other conventions | they move with the section normalization (X1), the gradient convention (X2) and the orientations (X5), all fixed by the packet |
| Slot selection, OQ1, the stress tensor, the metric, M8.7's gate | out of scope |
