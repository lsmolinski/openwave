# M8.10 method note: the first correction at the level-6 critical rays

> Task record: [`../tasks/m8_10_task_details.md`](../tasks/m8_10_task_details.md) (frozen claims, go-time pre-registration, deviations log). Roadmap: [`../m8_roadmap.md`](../m8_roadmap.md) M8.10. Run 2026-09-13, go 09:37 EDT, maintainer-run. Label earned: **blind** in the roadmap's sense ([§ CONVENTIONS](../m8_roadmap.md#conventions)): no agent saw a claimed value.

## 1. Equations first

### 1.1 The problem

On sections of the flat unitary bundle `E_σ` over `X = S³/Γ`, `σ ∈ {3′, 4}`, with `Γ` generated by the two unit quaternions of the handout ([`../m8_10/worklist_as_run.md`](../m8_10/worklist_as_run.md) § 1.2):

```text
(−Δ − λ) ψ + g N(ψ) = 0,        N(ψ) = |ψ|² ψ   (pointwise)
```

Level `n` carries `−Δ = n(n+2)`, the Haar measure has `∫_X 1 = 1`, and the block `K` is the seven-dimensional level-6 space. A fibre vector `v ∈ V_3` lifts to the block section `Φ = Φ_{σ,v}`, normalized so that `∫_X |Φ|² = 1`.

### 1.2 The expansion and the three equations the run computes

```text
ψ = aΦ + a³ξ + a⁵ζ + …,      λ = 48 + λ₂ a² + λ₄ a⁴ + …,      ξ, ζ ⊥ K

order a³, block:      λ₂ Φ = g Π_K N(Φ)            ⇒  λ₂ = g κ,  where Π_K N(Φ) = κ Φ
order a³, range:      A ξ = −g Π_⊥ N(Φ),   A = −Δ − 48
                      ‖Π_n ξ‖² = g² ‖Π_n N(Φ)‖² / (n(n+2) − 48)²
order a⁵, block:      λ₄ Φ = g Π_K DN_Φ[ξ]         (consistency: the part ⊥ Φ must vanish)
                      λ₄ = g ⟨Φ, DN_Φ[ξ]⟩ = −3 g² Σ_n ‖Π_n N(Φ)‖² / (n(n+2) − 48)
```

with `DN_Φ[h] = 2 Re(Φ†h) Φ + |Φ|² h`. The sum runs over the sector's levels above 6, and the cubic of a level-6 section has no content above level 18.

### 1.3 The block cubic, and the rays

```text
Π_6 N(Φ_{σ,v}) = Φ_{σ, w(v)},    w(v) = (d/7) ‖v‖² v − ((7−d)/√91) M₆(v),    d = dim σ
M_K(v) = [[v ⊗ Θv]_K ⊗ v]_3
```

`R1 = v_3`, `R2 = v_0`, `R3 = v_2 + v_{−2}`, `R4 = v_3 + v_{−3}` (coherent, zonal, octahedron, hexagon) and `R5 = cos t·v_2 + sin t·v_{−3}`, `sin²t = 12/25` (the pentagonal pyramid, the negative control).

## 2. What the agents derived, none of it supplied

| Result | Solver | Auditor |
| --- | --- | --- |
| Sector levels (B1) | DERIVED from exact characters in `Q(√5)` | DERIVED from exact characters and commuting projectors |
| `w(v)` (A1) | exact, in a four-dimensional basis, with the relation to the seven `M_K` (which span only four dimensions) | exact, through the right-invariant parts `K = 0, 6` |
| The sign of `λ₄` (C2) | Cauchy-Schwarz: `κ = ∫\|Φ\|⁴ < (∫\|Φ\|⁶)^{1/2}` unless `\|Φ\|` is constant, and `κ ≠ 1` | nonnegative terms from the structure, strictness from an exact nonzero level-18 term at every ray |
| The zeros (B3b) | a parity rule (`R2`), the octahedral stabilizer (`R3`), an exact Clebsch-Gordan cancellation (`R4`, level 16) | the same parity rule and stabilizer, and time reversal for level 16 at `R2`, `R3`, `R4` through a Jacobian identity; it also shows `R4`'s stabilizer alone does not force that zero |
| The `R5` breakdown (D2) | found: the order-`a⁵` block part has a component orthogonal to `Φ`, so the fixed-ray expansion fails there | found, with the same exact values |

Both agents reached the `R5` breakdown with the handout asking the same questions at all five rays, so nothing in the instrument marked `R5` as the control.

## 3. Equation-to-code map

Reproduction: each folder's `run_all.sh`, run from inside the folder with a `py` beside it that starts Python 3.12 without site processing, with numpy, scipy, sympy and mpmath on the path and the math libraries pinned to one thread. The solver's run takes about 90 seconds.

### 3.1 The solver, [`../scripts/m8_10_solver/`](../scripts/m8_10_solver/)

| Equation | Code |
| --- | --- |
| `Γ` from the generators, perfectness, `⟨3 3; 3 −3 \| 6 0⟩`, levels (X0, B1) | [`item0_1.py`](../scripts/m8_10_solver/item0_1.py), on the exact `Q(√5)` quaternions of [`common.py`](../scripts/m8_10_solver/common.py) (`Q5`, `close_group`, `generate_gamma`) |
| Sections as Peter-Weyl coefficient arrays, products by Clebsch-Gordan | [`engine.py`](../scripts/m8_10_solver/engine.py) `product`, `inner`, `conjf`, at 60, 110 and 160 digits |
| Exact sector projector, `w(v)`, `κ`, the level norms, `λ₄` (1.2, 1.3) | [`exact_parts.py`](../scripts/m8_10_solver/exact_parts.py) `Tvec`, `Op`, `M6J`, `exact_rational` |
| Rational identification at two precisions | [`identify.py`](../scripts/m8_10_solver/identify.py) |
| The seven `M_K` span four dimensions | [`relations.py`](../scripts/m8_10_solver/relations.py) |
| Reasons for the zeros (B3b) | [`item5_zeros.py`](../scripts/m8_10_solver/item5_zeros.py) `stab_mult`, `couple` |
| CG-free cross-check by exact-degree quadrature on `SU(2)` | [`quadcheck.py`](../scripts/m8_10_solver/quadcheck.py) |
| Order-`a³` residual with an explicit Casimir (X4) | [`engine.py`](../scripts/m8_10_solver/engine.py), with a wrong-factor control |

### 3.2 The auditor, [`../scripts/m8_10_audit/`](../scripts/m8_10_audit/)

| Equation | Code |
| --- | --- |
| `Γ`, classes, levels (X0, B1) | [`item01.py`](../scripts/m8_10_audit/item01.py) on [`lib.py`](../scripts/m8_10_audit/lib.py) (`Q5`, `closure`, `conj_classes`) |
| Its own coupling coefficients and `D^j`, cross-checked by a second construction | [`lib.py`](../scripts/m8_10_audit/lib.py), [`check_cg_D.py`](../scripts/m8_10_audit/check_cg_D.py) |
| Exact sector projectors from a class sum (commutant) | [`sectors.py`](../scripts/m8_10_audit/sectors.py) `exact_projectors` |
| `w(v)`, `κ`, the level norms, `λ₄`, the block projection at order `a⁵` | [`algebra.py`](../scripts/m8_10_audit/algebra.py) `right_parts`, `left_parts`, `M_K`, `mu_of` |
| Second route: sections as explicit functions of a unit quaternion, no Clebsch-Gordan | [`functional.py`](../scripts/m8_10_audit/functional.py) `DJ_grid`, `coeffs` |
| Reasons for the zeros | [`item5.py`](../scripts/m8_10_audit/item5.py), [`item5b.py`](../scripts/m8_10_audit/item5b.py) |
| Route comparison and item 8 | [`report.py`](../scripts/m8_10_audit/report.py) |

## 4. Results against the frozen claims

Designer comparison, scripted against the claims parsed from the frozen task document: 12 of 12 mechanical checks pass for each agent, and the two argument-graded claims pass on the designer's reading.

| Claim | Solver | Auditor | Verdict |
| --- | --- | --- | --- |
| X0 packet check | order 120, perfect | order 120, perfect | ✅ precondition met |
| A1 the block cubic | both coefficients, both sectors | both coefficients, both sectors | ✅ |
| A2 `λ₂/g` at eight expansions | all eight | all eight | ✅ |
| B1 level sets, level 18 twice in `4` | DERIVED | DERIVED | ✅ |
| B2 the 36 level norms | all 36 exact | all 36 exact | ✅ |
| B3a the zero census | matches | matches | ✅ |
| B3b an exact reason for every zero | every zero | every zero | ✅ |
| C1 `λ₄/g²` at eight expansions | all eight | all eight | ✅ |
| C2 `λ₄ < 0`, argued | general argument | structure plus exact entries | ✅ |
| C3 four distinct sector ratios | all four | all four | ✅ |
| D1 persistence at the four rays | orthogonal part exactly 0 | orthogonal part exactly 0 | ✅ |
| D2 `R5` negative control | nonzero in both sectors | nonzero in both sectors | ✅ |
| X4 order-`a³` residual | 1e-110 at 110 digits, control 0.007 to 0.2 | 2e-14 pointwise, 2e-10 by finite differences | ✅ |

The values the agents report at `R5` for items 4, 7 and 8 are not graded; the two agents agree on all of them exactly.

## 5. Adversarial audit record

The auditor committed its method ([`METHOD.md`](../scripts/m8_10_audit/METHOD.md)) and its own results ([`audit_stage1_return.md`](../scripts/m8_10_audit/audit_stage1_return.md)) before it saw the solver's work, then received that work for refutation ([`audit_stage2_return.md`](../scripts/m8_10_audit/audit_stage2_return.md)).

### 5.1 Reproduction and verdicts

The solver's `run_all.sh`, rerun from a copy, finished in 87 seconds and regenerated `results.json` with no difference. Items 0 to 8 are CONFIRMED value by value; item 9 is PARTIAL, for the reason in 5.2.

### 5.2 Checks that cannot fail

Each mutation ran in a fresh copy of the solver's code.

| Mutation | Result |
| --- | --- |
| M1: scale `N(Φ)` at three levels inside the engine | `λ₄` moved by up to a factor of 5 and the item 9 residual stayed at `7.9e-31`: the residual is built from the same level coefficients that define `ξ`, so it holds for any `N`. It still checks what X4 states, that a mislabeled level fails (the solver's wrong-factor control does); it cannot see an error in `N` itself. The auditor's finite-difference check, which evaluates `N(Φ)` pointwise, catches M1 at residuals up to 0.55 |
| M2: perturb one 110-digit value by `1e-40` relative | the identification failure is recorded, and the run exits 0 |
| M3: drop the conjugate term of `DN` in the quadrature cross-check | a deviation of `2.7e-3` is recorded and never asserted |
| M4: point an item 5 control at a zero level | it prints `False` and exits 0 |

No number is affected: the shipped run has an empty failure list, and the auditor reproduced each value by its own route. The auditor's own coefficient-space item 9 check shares M1's blind spot, as its stage-1 return already says.

### 5.3 The block cubic's form is not unique

The seven maps `M_K` span a four-dimensional space (rank 4, singular values 109, 40.7, 25.7, 23.7, then `1e-14`). The solver found this and gave the exact relation table; the auditor confirmed it and corrected its own stage-1 wording, which had called the vanishing `M_1` to `M_5` coefficients forced. A1's frozen form is one valid representation, and no value moves.

### 5.4 The arguments, compared

| Point | Finding |
| --- | --- |
| Level 16 at `R2`, `R3`, `R4` | time reversal forces it: with `Θv = ±v`, the level-6 coupling is proportional to `f²` and the level-8 coupling to the Jacobian `(f², f)_1 = 0` (auditor). The solver's exact two-term cancellation at `R4` is rigorous; its "not forced by symmetry" holds only for the stabilizer |
| The sign of `λ₄` | both arguments are complete. The solver's needs only `κ > 1`, since `\|Φ\|²` has levels 0 and 12 only, so `κ = 1 + ‖Π₁₂\|Φ\|²‖²` |
| The sector ratio of `κ − 1` | `16/9` at every ray, derived by both from `P_σ = (d/7) I + P^(6)` with `P^(6)` changing sign between the sectors |
| `R5` | parallelism at order `a³` is not forced by symmetry there and holds by exact evaluation; the order-`a⁵` orthogonal part lies along `e = sin t·v_2 − cos t·v_{−3}` with coefficient `−7188839√91/81061695000` in `3′` and `+19565553√273/384292480000` in `4`, opposite in sign |

### 5.5 One audit note that does not stand

The auditor recorded the solver's cross-check of its coupling coefficients against a library routine as a breach of the brief. The rule against library coupling tables was in the auditor's own brief, not the solver's ([`brief_solver.md`](../m8_10/brief_solver.md)), so it is not a deviation.

## 6. Containment record

| Check | Result |
| --- | --- |
| Canary | the user-global instruction file, a project instruction file and a memory index load unavoidably; none carries a value, equation, ray or claim |
| Solver transcript | 49 tool calls, none outside its room |
| Auditor transcript | 90 tool calls across both stages; the only paths outside its room are references to the output files of its own ten background commands, written there by the tool harness (deviations log) |
| Audit pattern | mutation-tested before use on eight synthetic out-of-room calls, all flagged, and five in-room calls, none flagged |

## 7. Provenance

The author's package landed after this verdict, at [#550](https://github.com/openwave-labs/openwave/pull/550), and the comparison against it is recorded in the task doc's [provenance comparison](../tasks/m8_10_task_details.md#provenance-comparison-2026-09-13). Agreement with it is weak evidence, since a shared convention error survives it, and disagreement is strong evidence.

## 8. What this run does NOT verify

| Not verified | Why |
| --- | --- |
| Branches at finite amplitude, their existence or stability | the run computes formal coefficients through order `a⁵` only |
| Anything beyond `λ₄` and the first state correction | no higher order was computed |
| The two further critical rays of Lemma 5.3(b) other than `R5` | the trigonal prism was not in the packet |
| Values under other conventions | they move with the section normalization (X1) and the gradient convention (X2), both fixed by the packet |
| Slot selection, OQ1, the stress tensor, the metric, M8.7's gate | out of scope by the task's own statement |
