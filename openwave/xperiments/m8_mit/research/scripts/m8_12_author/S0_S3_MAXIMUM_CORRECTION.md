# S0_S3_MAXIMUM.md: a dated correction

2026-09-22. The note is pinned by hash in three places: the manifest beside it, `prereg/s1_prereg_build.py`, which checks it on every run, and M8.13's task doc, which pins it as the argument under audit. Its bytes therefore stay as they are, and this file carries the correction beside them, as M8.11's disclosure correction was entered in M8.12's task doc rather than by editing M8.11's frozen text.

## The correction

The note's third sentence, "Every step is checked exactly, or armed numerically, by `check_s3_maximum.py` (30 checks, 0 failures; log `check_s3_maximum_log.txt`)", overstates what the checker covers. So does the checker's own opening line, "Proof chain, each link checked exactly (sympy) or armed". The count is accurate: 27 `gate()` call sites produce the 30 executed checks, 0 failures, and the script reproduces its pinned log byte for byte, which [#586](https://github.com/openwave-labs/openwave/pull/586) confirmed on an independent toolchain.

What the script does not gate, as M8.13's adjudication records and as its check labels show: `TrN̄ = 12`; the variational identity behind `‖Q‖ = max_E Tr(QE)`; the equivariance that licenses rotating `E` to diagonal form; the swap of the two maxima; any block's equality locus; step 3d; step 3e; and the `⊆` direction of step 4. Two checks are weaker than their labels: at lines 86 and 87 the script verifies the constant identities `3/2 + 36/8 = 6` and `15/√6 − 2√(3/2) = 9/√6`, not the two maximizations over the ellipse that those labels state. Line 61 is inert, since `e1 - e2` is not an atom of the expression it substitutes into; line 62 rebuilds the operator directly, so nothing downstream depends on it. The sweep excludes the 192 of its 20001 points lying within 0.01 rad of an axis, the exclusion the note's own "What the check script does" section states, so it bounds the maximum away from the three equality directions and says nothing inside those neighborhoods.

So the note's argument was never checked end to end by that script, and its equality set in particular rested on the note's own tracing, corroborated by a sweep that skips the neighborhoods where equality holds.

## What M8.13 supplied

M8.13 audited the argument. An offline auditor reached the same result by its own route before seeing the note, then graded the note step by step with no GAP and no DEFECT, eight of ten parts ESTABLISHED, SUPPLIED. It supplied the `U(1)` type restriction in step 1; the variational identity, the equivariance, the max-swap and the block basis in 3a; the on-ellipse maxima behind `M₁`'s and `M₃`'s bounds in 3b; the derivation of the sign conditions, the `a + 6b ≤ 0` branch, `M₂`'s equality locus and that `M₃`'s locus meets the ellipse at all in 3c; the whole `E* = Q/‖Q‖` chain of 3d, which the note does not state; the eigenspace computation and its multiplicity in 3e; and `|a₀₀|² = 4|α|²|β|²/7` on the span in step 4. The task doc's § ADJUDICATION carries that list and says whose argument closed which part, and records G2's uniqueness clause as an audited argument with those parts named. The manifest beside this file, in "One argument this package carries that the run did not settle", says that clause rests on the author's argument until an audit checks it; that is what M8.13 answered.

## Two refinements, neither a defect

- **`M₂` = `M₁` at `−b` holds up to basis orientation.** In the parity-adapted basis `M₂` is `diag(1, −1)`-conjugate to `M₁(−b)`, with the same characteristic polynomial. The auditor and the adversarial audit found this independently, and neither graded it a defect: only spectra are used, and the note fixes no basis.
- **Saturating the nematic bound is necessary for both extrema and sufficient for neither.** `TrN̄² = 171/2` holds identically on the whole plane `span{v₃, v₋₃}`, where `r̂₆ = 2|α|²|β|² + 1/924` runs from the minimum `1/924` to the maximum `463/924`. The checker's own lines 116 and 117 record both facts. Step 3 therefore narrows the problem to that plane and constrains `r̂₆` not at all within it; step 4's `⟨f⟩ = 0` and `Θu ∝ u` are what select the hexagon orbit. That same section of the manifest reads step 3 as carrying the substantive equality case and step 4 as combining them, which undersells step 4 the same way: both are load-bearing, for different things.
