# N2 checkpoint , closed-loop sim + mixing observables ✅ (LOCAL)

Script: `sandbox_v10/n2_closed_loop.py` · summary: `n2_closed_loop_summary.json` · plot: `n2_closed_loop.png`

## Result: N2 FOUNDATION PASS

### (A) closed-loop seeder + line tension

| R (vox) | loop length 2*pi*R | signed energy E(R) |
| --- | --- | --- |
| 5 | 31.4 | -5.46456e+04 |
| 7 | 44.0 | -5.45813e+04 |
| 9 | 56.5 | -5.45033e+04 |
| 11 | 69.1 | -5.44054e+04 |
| 13 | 81.7 | -5.43102e+04 |

Line tension `dE/dL = +6.74` (robust, monotone) -> the BARE loop has a collapse force and
SHRINKS. Stabilization (a twist / boost-dressing GEM dip, cf #200 boost-GEM finding, or a
balancing mechanism) is the honest open engineering problem flagged for N3+. Absolute E
includes the bulk LdG vacuum offset; the physical quantity is the slope (offset-independent).

### (B) observable pipeline (validated vs #199)

| angle | N2 extraction | #199 / TBM expected |
| --- | --- | --- |
| theta12 | 35.264 deg | 35.264 ✅ |
| theta23 | 45.000 deg | 45.000 ✅ |
| theta13 |  0.000 deg |  0.000 ✅ |

The standard PDG extraction (`sin^2 th13=|U_e3|^2`, ...) reproduces the tribimaximal angles
exactly -> the observable pipeline is correct and matches our own #199 scorecard.

### (B) the delta -> theta13 channel (EXPOSED)

Two-level activation `tan(2 theta13) = 2 delta V_loop / gap` (degeneracy-lifting; theta13 -> 0
as delta -> 0). With representative O(1) loop coupling (V_loop=gap=1):
- theta13(delta_phys=1e-10) = 5.73e-9 deg (linear onset ~ delta)
- delta needed for the NuFIT reactor angle 8.5 deg = 0.153

## ⚠️ Central quantitative tension (the real open problem, for N3)

Linear onset + O(1) loop coupling gives theta13 ~ 1e-8 deg at delta~1e-10, vs the observed
8.5 deg. Reproducing 8.5 deg needs EITHER delta ~ 0.15 (not 1e-10) OR a coupling enhancement
of ~1.5e9 (a near-degeneracy / resonance). **N3 must resolve which**: is the effective delta in
the mixing really 1e-10, or is theta13 sourced by a near-degenerate gap that resonantly
amplifies a tiny delta? This is the sharpest question the foundation hands to the search.

## How N1 and N2 connect

The BARE loop is undressed (g sits in the decoupled time block, the director field does not
vary it) -> no 1e20 range -> N1's precision method is not yet needed. It becomes ESSENTIAL the
moment the loop is boost-dressed (the dynamical / theta_13-relevant regime), where the g-axis
mixes into the varying spatial structure and the delta-sector (the theta_13 channel) drops
below the f64 floor. N1 proved that channel is recoverable to machine precision; N2 built the
loop + the observable that reads it. Together = the machine N3 drives.

## Status
N2.1 ✅ closed-loop seeder · N2.2 ✅ flavour triads + SO(3) + mixing observables (TBM-validated,
delta->theta13 exposed) · N2.3 ✅ loop energy + line tension (stability flagged). Next: findings doc.
