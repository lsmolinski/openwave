# N1 checkpoint , precision-safe numerical method ✅ (LOCAL)

Script: `sandbox_v10/n1_precision_method.py` · summary: `n1_precision_method_summary.json` · plot: `n1_precision_method.png`

## Result: CANCELLATION TEST PASS

The O(delta) SO(3)-breaking coefficient E_1 (the theta_13 channel) computed three ways:

| Method | E_1 (tiny grid, n=6) | rel err vs reference |
| --- | --- | --- |
| naive f64 finite difference | `0.000e+00` (annihilated) | `1.0` (100% wrong) |
| graded perturbative (the method) | `7.4761884549e+28` | `9.4e-16` (machine) |
| mpmath 50-digit reference | `7.4761884549e+28` | , (gold truth) |

## Why naive fails (the catastrophe, quantified)

| Quantity | Value |
| --- | --- |
| delta-graded energy orders E_0 : E_1 : E_2 (full grid n=20) | `3.16e+39 : 2.99e+29 : 3.78e+20` |
| spacing between orders | ~`g = 1e10` per order (the breaking couples through the g-axis) |
| breaking energy signal `delta_phys * |E_1|` | `2.99e+19` |
| ULP of E_0 (f64 resolution at the g-sector) | `6.04e+23` |
| signal below ULP? | YES , so naive `[E(delta)-E(0)]/delta` underflows to 0 |

The breaking lives 4+ orders of magnitude below the floating-point resolution set by the
g-sector. Any single-accumulator f64 (or even f128) energy sum DESTROYS the theta_13 physics.

## The method (what makes it work)

1. **Non-dimensionalization**: g is a fixed background scale; the energy grades cleanly in
   powers of g (E_0 ~ g^4, E_1 ~ g^3, E_2 ~ g^2 in these bare units).
2. **Perturbative-delta**: `D(delta)=D_0+delta.D_1` linear => `M=M_0+delta.M_1` exact =>
   the polynomial energy expands exactly via the delta-graded commutators
   `C = C_0 + delta.C_1 + delta^2.C_2` (each held separately). E_1 = `4.c^2.sum 2<C_0,C_1>_s`
   + the graded potential order-1, never summed against the 1e39-scale E_0.
3. **Convention**: Duda index-0, D=diag(g,1,delta,0), eta=diag(-1,1,1,1).

## Physical payoff for the program

E_1 IS the leading SO(3)-breaking = the theta_13 channel (10a "the connecting hypothesis").
N1 proves that channel is computable to machine precision , N4 (theta_13) is numerically
reachable. Without this method the entire delta-sector is unrecoverable noise.

## Status
N1.1 ✅ index-0 + energy functional · N1.2 ✅ cancellation test (3-way) · N1.3 ✅ non-dim +
delta-expansion. Next: N2 closed-loop seeder + flavour states + SO(3) mixing observables.
