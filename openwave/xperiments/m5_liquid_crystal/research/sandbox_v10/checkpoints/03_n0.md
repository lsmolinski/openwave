# N0 checkpoint , engine <-> port equivalence ✅ (LOCAL)

Script: `sandbox_v10/n0_engine_equivalence.py` · summary: `n0_engine_equivalence_summary.json`
Built in response to Rodrigo's question: "the scripts don't import OpenWave , do they embed the
LC model, and how do we claim a connection to M5?" Answer: the port DID transcribe the engine's
energy functional; N0 now VERIFIES that transcription against the live engine.

## Result: EQUIVALENCE PASS

### Test A , fidelity (engine kernel vs numpy port, identical field, toy scales g=8 delta=0.30)

| Quantity | Value |
| --- | --- |
| E_engine (`compute_energyH_density_M`) | `5.41607369e-06` |
| E_port (numpy Frobenius, index-3) | `5.41625635e-06` |
| relative difference | `3.37e-05` (< 1e-4 tol) |

The 3.4e-5 gap is exactly the expected f32(engine) vs f64(port) rounding on the f32-seeded
field. => the numpy port is a faithful re-implementation of the M5 LdG energy. The foundation's
"validated against the M5 liquid-crystal model" claim is earned.

### Test B , convention invariance (engine index-3 vs Duda index-0, same physical field)

| Quantity | index-3 (engine) | index-0 (Duda) | rel diff |
| --- | --- | --- | --- |
| signed curvature | `2.80003267e-06` | `2.80003267e-06` | `0.0` (bit-identical) |
| LdG potential | `-2.72831121e-03` | `-2.72831121e-03` | `0.0` (bit-identical) |

Relabel `sigma=[3,0,1,2]` (Duda time 0 = engine time 3) maps one to the other EXACTLY. => the
index choice is a pure relabeling with ZERO physics consequence.

## What N0 settles

1. The standalone numpy foundation is tied to the actual engine (transcription verified).
2. The engine's index-3 convention is NOT wrong , it is physically identical to Duda's index-0.
   This directly informs the convention-housekeeping decision (see findings + the recommendation):
   the fix is documentation + a consistency audit, NOT a risky rewrite of validated engine code.

## Status
N0 ✅ (added on top of N1 ✅ + N2 ✅). The M5 connection is verified. Convention recommendation
recorded for Rodrigo's call.
